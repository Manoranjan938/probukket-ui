import { type ReactElement } from "react";

const BoardView = (): ReactElement => {
  return (
    <section className="board_view_section">
      <div className="board_view_header">
        <div className="board_view"></div>
      </div>
    </section>
  );
};

export default BoardView;
