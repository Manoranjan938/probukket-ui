import React, { type ReactElement } from "react";

const CalenderHeader = (): ReactElement => {
  return (
    <section className="calender_header_section">
      <h1>Hello from headers section</h1>
    </section>
  );
};

export default CalenderHeader;
