import { type ReactElement } from "react";

const Footer = (): ReactElement => {
  return (
    <section className="container footer_section">
      <h1>Footers</h1>
    </section>
  );
};

export default Footer;
